#include "main.h"

/**
 * Wraps a controller value above 20 and below -20.
 * dewine is a wrapper for dewined(x, 20);
 */
int dewined(int x, int y);
int dewine(int x);

/**
 * Returns the maximum of two values.
 * max is a wrapper for maxd(x, 127);
 */
int maxd(int x, int y);
int max(int x);

/**
 * Returns the minimum of two values.
 */
int min(int x, int y);
